fastapi==0.115.6
uvicorn[standard]==0.34.0
langgraph>=0.2.0,<0.4.0
openai>=1.50.0          # Groq is OpenAI-compatible; same SDK, different base_url
chromadb>=0.5.0         # local persisted vector store (default ONNX MiniLM embeddings)
httpx>=0.27.0           # CoinGecko calls
python-dotenv>=1.0.0

# --- Phase 2 infra ---
redis>=5.0.0                 # price/question cache + per-IP rate limit
SQLAlchemy>=2.0.0            # request persistence
psycopg[binary]>=3.1.0       # Postgres driver (binary wheel; no system libpq needed)
langfuse>=2.0.0,<3.0.0       # optional tracing; v2 client API
